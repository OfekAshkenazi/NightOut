<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center">

    <CostumeViews.ImageRecyclerView
        android:id="@+id/imagePager_gallery_dialog"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true" />

    <com.rd.PageIndicatorView
        android:id="@+id/indicators_ImageDialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        app:piv_dynamicCount="true"
        app:piv_selectedColor="@color/purple_club"
        app:piv_unselectedColor="@color/colorAccent"
        app:piv_viewPager="@id/imagePager_gallery_dialog" />

    <ProgressBar
        android:id="@+id/progressBar_imageDialog"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_centerInParent="true" />
</RelativeLayout>